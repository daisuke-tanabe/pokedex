---
name: coding-style
description: 言語・フレームワーク非依存のコーディングスタイル規範。可読性最優先、KISS / DRY / YAGNI、真偽値の否定形を避ける命名、ファイル構成 (1 ファイル 200〜400 行、最大 800 行)、関数設計 (副作用回避・早期リターン・マジックナンバー・長い関数・ネスト三項・不要な if 文)、境界処理 (入力バリデーション・エラーハンドリング) を扱う。コードを書く・編集する・レビューする・リファクタリングする作業、関数 / クラス / モジュールの新規作成や既存修正、code smell (深いネスト・長い関数・マジックナンバー・ネスト三項) に気づいた瞬間、boolean フラグや早期リターンの判断を迫られた場面では、たとえユーザーが「コーディング規約」と明言しなくても必ず参照する。
paths:
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
  - "**/*.mjs"
  - "**/*.cjs"
  - "**/*.py"
  - "**/*.go"
  - "**/*.rs"
  - "**/*.rb"
  - "**/*.java"
  - "**/*.kt"
  - "**/*.swift"
---

# コーディングスタイル

言語・フレームワークに依存しない、コード全般の書き方の原則をまとめる。

## 可読性最優先

- 明確な変数名と関数名を使う
- コメントよりも自己説明的なコードを優先する
- 一貫したフォーマットを保つ
- 過剰な単純化で可読性・保守性を犠牲にしない

## KISS（Keep It Simple）

- 実際に動く最もシンプルな解決策を選ぶ
- 早すぎる最適化を避ける
- 賢さや短さよりも明快さを最適化する

## DRY（Don't Repeat Yourself）

- 繰り返されるロジックは共有関数・ユーティリティに抽出する
- コピーペーストによる実装ドリフトを避ける
- 反復が現実のものになったら抽象化する。投機的な抽象化はしない

## YAGNI（You Aren't Gonna Need It）

- 必要になる前に機能や抽象化を作らない
- 投機的な汎用化を避ける
- シンプルに始め、現実に圧力がかかってからリファクタリングする
- 既に有用な抽象や責務分離を、行数削減のために潰さない

## 命名

- 真偽値の名前に否定形 (`not`) を含めない (例: `isNotActive` ではなく `isInactive`)

## ファイル構成

多くの小さなファイル > 少ない大きなファイル：
- 高凝集・低結合
- 通常200〜400行、最大800行
- 大きなモジュールからユーティリティを抽出する
- タイプ別ではなく、機能・ドメイン別に整理する

## 関数設計

関数を書くときの基本姿勢と、避けるべき書き方をまとめる。

### 副作用を避ける

関数は引数と戻り値だけで完結させる。

- 関数のスコープ外の変数を変更しない
- 引数のオブジェクト・配列を変更しない（新しいインスタンスを返す）
- 副作用が必要な場合はその意図を明示する

### 避けるコードスメル

#### 深いネスト

ロジックが積み重なり始めたら、ネストした条件分岐より早期リターンを優先する。

#### マジックナンバー

意味のある閾値・遅延・上限には名前付き定数を使う。

#### 長い関数

大きな関数は責務が明確な小さな単位に分割する。

#### ネストした三項演算子

三項演算子のネストは可読性を著しく損なう。
複数条件の分岐は switch 文・if/else・早期リターンを使う。

#### 不要な if 文

条件によって boolean 値や同じ呼び出しを切り替えるだけなら、if 文は不要。
値を直接渡すか、三項演算子を式として使う。

## 境界処理

外部とのやりとり (ユーザー入力 / 外部 API / ファイル等) で守るべき規範をまとめる。

### 入力バリデーション

常にシステム境界でバリデーションを行う：
- 処理前にすべてのユーザー入力をバリデートする
- 利用可能な場合はスキーマベースのバリデーションを使用する
- 明確なエラーメッセージでフェイルファストする
- 外部データを信頼しない（APIレスポンス、ユーザー入力、ファイルコンテンツ）

### エラーハンドリング

常に包括的にエラーを処理する：
- 各レベルで明示的にエラーを処理する
- UI向けコードではユーザーフレンドリーなエラーメッセージを提供する
- サーバー側では詳細なエラーコンテキストをログに残す
- エラーを無視して握り潰さない

